
@model DeltaQrCode.ViewModels.HotelListViewModel

<table class="table table-bordered" style="width:100%;border:1px solid black;">
    <thead>
        <tr>
            <th scope="col">Actiune</th>
            <th scope="col" style="width:50px;">Nr. Auto</th>
            <th scope="col">Client</th>
            <th scope="col">Marca</th>
            <th scope="col">Nr.Buc</th>
            <th scope="col">Dimensiuni</th>
            <th scope="col">Uzura</th>
            <th scope="col">Tip</th>
            <th scope="col">Evaluare</th>
            @*<th scope="col">Rand</th>*@
            <th scope="col">Poz</th>
        </tr>

    </thead>
    <tbody>
        @foreach (var item in Model.List)
        {
            <tr>
                <td>
                    <div class="btn btn-outline-info actionButton" data-value="@item.SetId" data-actionType="info"><i class="fas fa-info-circle"></i></div>
                    <div class="btn btn-outline-warning actionButton" data-value="@item.SetId" data-actionType="edit"><i class="far fa-edit"></i></div>
                    <div class="btn btn-outline-danger deleteButton" data-value="@item.SetId"><i class="fas fa-trash-alt"></i></div>
                </td>
                <td class="col">@item.NumarInmatriculare</td>
                <td class="col">@item.NumeClient</td>
                <td class="col">@item.MarcaId</td>
                <td class="col">@item.NrBucati</td>
                <td class="col">@item.DimensiuniString</td>
                <td class="col">@item.UzuraString</td>
                <td class="col">@item.TipSezon</td>
                <td class="col">@item.Evaluare</td>
                @*<td class="col">@item.Rand</td>*@
                <td class="col">@item.PositionString</td>
            </tr>
        }
    </tbody>
</table>
<input id="currentPage" type="hidden" value="@Model.CurrentPage" hidden />
<div class="justify-content-center text-center">
    <ul class="pagination text-center justify-content-center ">
        @for (int i = 0; i < Model.TotalPages; i++)
        {
            <li class="button page-item CustomPagination @(Model.CurrentPage == @i + 1 ? " active " : string.Empty )" data-value="@(i + 1)"><div class="page-link">@(i + 1)</div></li>
        }
    </ul>
</div>
